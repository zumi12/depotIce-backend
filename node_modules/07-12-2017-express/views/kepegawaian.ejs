<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <table border="1">
    	<thead>
    		<th>nik</th>
    		<th>name</th>
            <th>pengajar</th>
            <th>alamat</th>
    	</thead>
    	<tbody>
    		<% for(var i=0; i < users.length; i++) { %>
        <tr>
        	<td> <a href="/kepegawaian/nik?no=<%= users[i].nik %>&nama=<%= users[i].nama %>" ><%= users[i].nik %></a></td>
        	<td><%= users[i].nama %></td>
        	<td><%= users[i].pengajar %></td>
            <td><%=users[i].alamat %></td>
        </tr>
                <% } %>
    	</tbody>
    </table>

    <a href="/">Back</a>
    <a href="/latiahan">latihan</a>
  </body>
</html>
